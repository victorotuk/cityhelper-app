/**
 * Theme tokens and shared class names for consistent UI (Track Item / add modal style).
 * Use these class names so cards, forms, and buttons match across Settings, Setup Wizard, modals, etc.
 */

/** Card style matching Track Item / item cards: gradient left bar, bg-card, border-visible, radius-lg */
export const CARD_CLASS = 'app-card';
export const CARD_HEADER_CLASS = 'app-card-header';
export const CARD_BODY_CLASS = 'app-card-body';

/** Form style matching Add Item modal */
export const FORM_GROUP_CLASS = 'form-group';
export const FORM_INPUT_CLASS = 'form-input';
export const FORM_LABEL_CLASS = 'form-label';

/** Buttons: primary = gradient (Track Item style), ghost = outline */
export const BTN_PRIMARY_CLASS = 'btn btn-primary';
export const BTN_GHOST_CLASS = 'btn btn-ghost';

/** Section wrapper for settings/setup pages */
export const SECTION_CLASS = 'settings-section';
export const SECTION_DESC_CLASS = 'section-desc';
